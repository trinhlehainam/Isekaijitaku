# Configuration for Docker macvlan host interface: {{ file_name }}
# Generated by Ansible from docker-macvlan.conf.j2

# Parent network interface on the host (e.g., eth0, enp3s0)
INTERFACE="{{ parent_interface }}"

# IP address to assign to the host's macvlan interface (e.g., 192.168.1.209)
# This IP must be in the same subnet as the parent_interface and outside DHCP/container ranges.
HOST_IP="{{ host_ip }}"

# Desired name for the host's macvlan interface (e.g., {{ macvlan_interface_name }})
MACVLAN_INTERFACE="{{ macvlan_interface_name }}"

# Array of IP CIDR blocks used by Docker containers on the corresponding Docker macvlan network.
# The setup script will add routes for these CIDRs via the host's MACVLAN_INTERFACE.
# Example: CONTAINER_IP_CIDR=("192.168.1.210/28" "192.168.1.240/30")
CONTAINER_IP_CIDR=({% for cidr in container_cidrs %}"{{ cidr }}" {% endfor %})