---
- name: Create a test file on the SMB share
  ansible.builtin.copy:
    content: "Test file created by Ansible."
    dest: "{{ smb_mount_point }}/ansible_test.txt"
    owner: "{{ smb_mount_uid }}"
    group: "{{ smb_mount_gid }}"
    mode: '0644'
  when: smb_share_is_mounted